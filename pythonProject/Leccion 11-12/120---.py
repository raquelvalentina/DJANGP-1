from cccc import *

def funcion ():
    a = float(input('Valor de la altura: '))
    b = float(input('valor de la bace: '))
    c = str(input('de que color es la figura: '))
    elmnto = rectangulo(a, b, c)
    print(elmnto)

funcion()